# LinkWheelVS

This extension registers repos for LinkWheel to serve a list of possible
actions, as well as provides a quick shortcut for linking to repo hosting
websites, such as GitHub and Helix Swarm.

## Features

* Right-click menu on code provides the option to copy a link to the remote
  host
* Registers repos to LinkWheel whenever a link is generated

## Requirements

Users must have LinkWheel installed. The extension uses `LinkWheelCli`,
which it expects to be on the system PATH.

## Release Notes

### 1.0.0

Initial release of LinkWheelVS